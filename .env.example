# Copy this file to `.env` or `.env.local` and fill values.
# Note: Vite only exposes variables prefixed with VITE_.

# Mapbox publishable token (safe to expose client-side)
VITE_MAPBOX_ACCESS_TOKEN=pk.YourMapboxPublicTokenHere
